import React from 'react';
import './header.css'

const Header = () => {
  return (
    <header className="container">
      <h2 className="header">Where creators run <br/>their business</h2>
      <button style={{border:'none'}}> Get Started </button>
      </header>
  );
};

export default Header;